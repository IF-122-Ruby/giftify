import React, { Component } from 'react';
import ReactDOM from 'react-dom';

export default class SearchRoman extends Component {
  getCompleteResults(element) {
    const rest = "fdfs";
    console.log(rest);
  }
  render () {
    return( <div>
        <h1> Hellow</h1>
    </div>);
  }
}

