= search_form_for @q, url: account_organization_gifts_path, class: 'input-group flex-nowrap' do |f|
  = f.search_field :name_cont, placeholder: "Search..."
  = f.submit 'Find', class: 'btn btn-secondary'
