.shadow-sm.p-0.bg-white
  .container-fluid.pt-3
    = link_to "new user", new_admin_user_path
    .row.d-flex.p-2.align-items-center.justify-conten-around.mb-5.mt-3
      - @users.each do |user|
        .col-2.d-none.d-sm-block.mb-2
          = image_tag user.avatar, class: "img-fluid user-avatar"

        .col-7
          .col
            = user.full_name

        .col-2.d-flex.justify-content-around.flex-wrap
          = link_to "Show", admin_user_path(id: user.id)
          = link_to 'Edit', edit_admin_user_path(id: user.id)
          = link_to 'Delete', admin_user_path(id: user.id), method: :delete, data: { confirm: 'Are you sure?'}

  .digg_pagination.pagination-position.pb-5
      = will_paginate @users, container: false
